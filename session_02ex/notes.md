# Post session 2 experiments

## Experiment 1
We will use objconv to decompile exit program from first session

Command:
`objconv -fgasm exit exit.s`

Now let's check source code generated by objconv, and if we can compile it.

I could with standard compile(as, ld), however I had to change int -128 into int 0x80